services:
  app:
    image: ghcr.io/boppescripting/aisles:latest
    restart: unless-stopped
    ports:
      - "${PORT:-3001}:3001"
    environment:
      NODE_ENV: production
      LIBSQL_URL: file:/app/data/aisles.db
      APP_PASSWORD: ${APP_PASSWORD:-}
    volumes:
      - aisles_data:/app/data

volumes:
  aisles_data:
